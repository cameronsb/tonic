# Findings Log

## Summary

Learnings, failures, and escalations from the ralph_20260714 P0 session. Lines containing `Fix Applied:`, `STUCK`, or `ESCALATE` are the aggregation markers.

## Detailed Findings

- 2026-07-14: Session ralph_20260714 started on branch `ralph/p0` — six P0 tasks queued from ROADMAP.md.

- 2026-07-15: Session ralph_p1_20260715 started on branch `ralph/p1` — six P1 core-interaction accessibility tasks queued from ROADMAP.md (P1 tier). P0 tier complete and merged to main.

- 2026-07-15: Session ralph_p2_20260715 started on branch `ralph/p2` — five remaining P2 state-architecture & rendering tasks queued from ROADMAP.md (P2-2 through P2-6; P2-1 SettingsProvider already complete and merged to main). New this tier: every task touching `src/` requires a Playwright browser smoke check after `npm run validate` and before commit (app renders, zero console errors, chord card plays, piano keys respond, settings drawer opens). Opus assigned to the two architectural items (P2-2 toggle-state dedup, P2-3 context split); sonnet to the three memoization items (P2-4 PianoKey memo, P2-5 PianoPreview hoist, P2-6 ChordStrip chord-list memo). P2-3 depends on P2-2; P2-4/5/6 depend on P2-3.

- 2026-07-15: Follow-up (not an escalation) — user testing of P1-1 (df96bba) found that rapid Enter+arrow navigation can leave a key visually stuck "pressed": keydown sets pressed state on key A, focus moves before keyup, keyup lands on key B, key A never clears. Queued as P1-7 (clear keyboard-pressed state on blur).

- 2026-07-16: Session ralph_p3_20260716 started on branch `ralph/p3` — six P3 type-safety, domain-correctness & test tasks queued from ROADMAP.md (P3 tier). P0, P1, and P2 tiers all complete and merged to main. Ordering rule enforced per ROADMAP § sequencing item 4: the behavior-pinning tests (P3-5: `getChordDisplayName`, `getConflictingModifiers`, `getBorrowedChords`) land FIRST, and the type refactors that touch those functions (P3-1, P3-2, P3-3) `depends_on` P3-5. P3-4 (minor scale-degree labels) and P3-6 (AudioContext cleanup) are independent (no P3-5 dependency). Model assignments: opus for cross-file type-fallout items (P3-2 ModifierLabel union + ChordModifier discriminated union, ripples into getChordDisplayName; P3-3 noUncheckedIndexedAccess, codebase-wide fallout); sonnet for localized items (P3-5 tests authoring, P3-1 getChordTypeFromIntervals return-type change, P3-4 label fix, P3-6 audio cleanup). Smoke-check exemption added for tests-only tasks (P3-5) since they change no runtime behavior. INTENTIONAL EXISTING-TEST CHANGE flagged: P3-4 must rewrite the scale-degree label assertions at `musicTheory.test.ts:244-254`, which currently PIN the buggy duplicate `'3'/'6'/'7'` behavior — this is the one place in the tier where changing existing test expectations is correct, and the worker must record it here when P3-4 lands.

- 2026-07-16: Discovered during P3 work that `npm run typecheck` (`tsc --noEmit`) is a silent no-op — the root `tsconfig.json` has `files: []` with project references, and plain `tsc --noEmit` does not follow project references (only `tsc -b` does). This means `npm run validate` (typecheck && lint && test), used by the pre-commit hook and CI gate, does NOT actually catch type errors; only the deploy build's `tsc -b` does. Fix Applied: queued as P3-7 (Make `npm run typecheck` actually typecheck) — worker to change the `typecheck` script to follow project references (`tsc -b --noEmit` or per-project `tsc -p ... --noEmit` invocations) and verify it genuinely fails on an injected type error.

- 2026-07-16: P3-3 done (commit 6e3dc3c). Enabled `noUncheckedIndexedAccess` in `tsconfig.app.json` — surfaced 52 errors across 11 files. Verified via `tsc -b --force` (NOT `npm run typecheck`, which is still the P3-7 no-op) plus full `npm run validate` (159 tests green) and the Playwright smoke check (app renders, 0 console errors, chord card + piano key + settings drawer all interact cleanly). Fix strategy per the roadmap: provably-in-range lookups get a `!` WITH a one-line justification comment, no blanket assertions. Concretely — `NOTES[expr % 12]` (index always 0-11 into the 12-element array), loop bodies where `i < arr.length`, and length-guarded access (`ConfigBar` focusable first/last after an early `length === 0` return; `getScaleDegreeNumeral`'s `chords.triads[scaleDegree]` after the `< length` guard). Two non-assertion fixes: (1) `useMidiInput` now bails (`if (command === undefined || note === undefined || velocity === undefined) return;`) on a truncated `event.data`, which also narrows the three bytes to `number` for the rest of the handler; (2) `ChordCard`'s selected check switched from `state.selectedChords.length > 0 && state.selectedChords[0].x` to optional chaining `state.selectedChords[0]?.x`. Source-level fixes that resolved consumer errors: `getScaleChords` (`scaleNotes[index]!`) fixed the `ChordStrip` → `ChordCard` `rootNote: Note | undefined` errors; `getBorrowedChords` already returns `Note` after asserting its 8 `NOTES[(rootIndex + n) % 12]` roots. Fixed the one affected test (`pianoUtils.test.ts` monotonicity loop `keys[i]!`) — additive assertion only, no behavior/assertion change. Fix Applied: no unjustified `!`; all assertions carry a range/guard comment.

- 2026-07-16: P3-4 done. Fixed `getScaleDegreeLabel`'s minor-mode chromatic labels in `src/utils/musicTheory.ts` — raised 3rd/6th/7th (intervals 4, 9, 11) now label `'♮3'`/`'♮6'`/`'♮7'` instead of the bare `'3'`/`'6'`/`'7'` that collided with the diatonic degree labels (interval 3 → `'3'`, 8 → `'6'`, 10 → `'7'`). **Intentional existing-test change** (per PLAN.md/ROADMAP.md P3-4, the one sanctioned exception to "never rewrite existing assertions"): updated the three pinning assertions at `src/utils/__tests__/musicTheory.test.ts` (formerly lines ~244-254, "Chromatic notes in minor keys" describe block) from `'3'/'6'/'7'` to `'♮3'/'♮6'/'♮7'`, and added one new test asserting the diatonic and raised labels for A minor's 3rd degree (C vs C#) are now distinct. Verified visually via the Playwright smoke check: switched to A minor, opened "Borrowed" chords, and confirmed the mini piano-preview SVGs on the borrowed chord cards (`IV D`, `VI F#`, `VII G#`) show `♮6`, `♮3`, `♮6`, `♮7` distinctly from plain `3`/`5`/`7` labels elsewhere. `npm run validate` green (tsc, lint 0 warnings, 160 tests). No production behavior beyond the label text changed.

- 2026-07-16: Checkpoint 2 user review caught two problems with P3-4's fix (611ca61): (1) theory — the app's diatonic minor degrees are labeled with bare `3`/`6`/`7`, so the raised chromatic tones in sharp-side minor keys (e.g. C#/F#/G# in A minor) are genuinely sharps, not naturals — a `♮` prefix was theoretically misleading; (2) legibility — the mini piano-preview labels render at 7-9px in JetBrains Mono (`.chord-card-key-label` in ChordCard.css), and the accidental glyph was an illegible ~5px smudge at that size. Fix Applied: queued and completed as P3-8 (Relabel raised minor degrees ♮→♯ and improve accidental legibility) — swapped `♮3`/`♮6`/`♮7` to `♯3`/`♯6`/`♯7` in `getScaleDegreeLabel`'s `minorChromaticLabels` map (with updated justification comments), updated the corresponding pinning assertions in `musicTheory.test.ts`, and bumped `.chord-card-key-label` font-size one notch at each responsive breakpoint (8→9 base, 9→10 at 1024px+, 7→8 at the 768px compact breakpoint) after confirming against `PianoPreview`'s SVG (`ChordCard.tsx`) that the black-key label box (`x + 4`, text-anchor middle) had headroom and wouldn't overflow into neighboring keys. Verified via `npm run validate` (160 tests green) and a live browser check: switched to A minor, confirmed via `browser_evaluate` over `.chord-card-key-label` that borrowed-chord preview labels now contain U+266F (♯) and zero U+266E (♮) anywhere in the DOM, and a zoomed screenshot of the `VI F#` card showed `♯3`/`♯6`/`♭2` clearly legible. Zero console errors across the session; dev server (already running on 5173 for the user's manual review) was reused and left running, not restarted.

- 2026-07-16: P3-9 done (commit a7de177). **User-directed intentional behavior change** (Checkpoint 2 product decision): `getBorrowedChords`' minor-mode branch was producing wrong chords. It hardcoded four MAJOR triads at offsets +5/+9/+11/+3, so for A minor it yielded IV=D maj (correct) but VI=F♯ maj and VII=G♯ maj — neither exists in the parallel major (A major contains F♯ **minor** and G♯ **diminished**) — plus III=C maj, a duplicate of the diatonic III card. Rewrote the branch to true modal interchange from the parallel MAJOR key: I (Picardy major tonic, +0, `[0,4,7]`, `maj`), IV (+5, `[0,4,7]`, `maj`), vi (+9, `[0,3,7]`, `min`), vii° (+11, `[0,3,6]`, `dim`); dropped the duplicate III. Numerals now encode quality by case (`I`/`IV`/`vi`/`vii°`), matching the diatonic-minor row's convention (`i`/`ii°`/`III`…). Verified nothing keys off the old borrowed numeral strings: onboarding targets the `.borrowed-toggle-btn` class (not a numeral), `LoadingOverlay` uses static tip strings, and `ChordCard`'s P0-6 reset effect keys on `numeral` as a plain primitive (new unique numerals are fine; separate React lists from the diatonic row so no key collision). **Intentional test change** (the P3-5 pinning tests deliberately pinned the old buggy output, per this user decision): rewrote the A-minor block of `getBorrowedChords.test.ts` to assert I=A maj, IV=D maj, vi=F♯m, vii°=G♯dim (rootNote + intervals + type + numeral) and added a guard that the old VI/VII/III numerals are gone; C-major (parallel-minor) assertions untouched. **Major-mode branch verified correct and unchanged**: for C major it borrows iv=Fm, ♭VI=A♭ maj, ♭VII=B♭ maj, ♭III=E♭ maj — all genuinely diatonic to the parallel minor (C natural minor), so it is already true modal interchange. Verified via `npm run validate` (tsc -b, lint 0 warnings, 161 tests green) and the Playwright smoke check on the user's already-running dev server (5173, reused and left running): switched to A Minor and confirmed the borrowed row renders I/A (labels 5·1·♯3), IV/D (4·1·♯6), vi/F♯m (1·♯3·♯6), vii°/G♯° (4·2·♯7) with the "Borrowed (4)" badge; clicked each of the four borrowed cards (played, zero console errors); confirmed C Major's borrowed row is unchanged (iv/Fm, ♭VI/G♯, ♭VII/A♯, ♭III/D♯); piano key A3 and the settings drawer both interact cleanly. Zero console errors throughout (only benign pre-existing MidiSysEx permission warnings). Note: the raised-tone roots (F♯/G♯ in A minor) correctly show ♯6/♯7-style preview labels from `getScaleDegreeLabel` (expected per P3-8).

- 2026-07-16: Session ralph_p4_20260716 started on branch `ralph/p4` — five P4 cleanup & hygiene tasks queued from ROADMAP.md (P4-1 through P4-5), the FINAL roadmap tier. P0, P1, P2, and P3 tiers all complete and merged to main. Ordering: the dead-code purge (P4-1) goes FIRST within the tier (ROADMAP § sequencing item 5's "purge last" constraint is about not conflicting with P0–P3, all merged; within P4 the other items build on the cleaned-up `musicTheory.ts`). `depends_on`: P4-2 (frequency consolidation) and P4-3 (domain-data move) both depend on P4-1 so they don't refactor around soon-deleted code; P4-5 (ResolvedChord type) depends on P4-3 so the named type lands on the already-table-driven `getBorrowedChords`; P4-4 (CSS hygiene) is independent. Model assignments: **opus** for the two cross-file items — P4-1 (surgical dead-code removal across `musicTheory.ts`/`types/music.ts`/`MusicContext.tsx`/`useMidiInput.ts`/`LearnMode.css`/`ChordStrip.tsx`/`package.json`, each symbol verified to have zero live refs first) and P4-3 (import-graph reshuffle moving domain tables into `config/`); **sonnet** for the mechanical items — P4-2, P4-4, P4-5. No tests-only tasks this tier, so every task runs the browser smoke check; P4-4 additionally requires before/after visual screenshots. **Already-resolved by earlier tiers, verified against current code and annotated in PLAN.md (do NOT redo):** (1) P4-1's `scaleViewEnabled` context field is already gone (consolidated by P2-2) — only `pianoRange`/`setPianoRange`/`toggleScaleView`/`deselectChords` remain dead; (2) P4-1's `typecheck` script is NOT broken (P3-7 fixed it to `tsc -b --noEmit`) — only `check:client`/`check:all` + the unconfigured `lint-staged` devDep are dead; (3) P4-3's `getBorrowedChords` MINOR branch was fully rewritten in P3-9 (true parallel-major I/IV/vi/vii°) and its tests already pin that output — P4-3 must table-drive the still-push-block function and standardize the MAJOR branch's ASCII `bVI`/`bVII`/`bIII` to the `♭` glyph while preserving P3-9's exact chord output; (4) P4-3's "write pinning tests first" prerequisite is already satisfied by P3-5/P3-9; (5) P4-2's canonical `midiToFrequency` already exists — the live inline duplicates to consolidate are `getChordFrequencies` and TWO copies in `pianoUtils.ts`.

- 2026-07-14: P0-4 done (commit b33b594). Adopted `useGlissando` for the touch path in `Piano.tsx`. Added a `triggerOnStart` option to the hook so it primes de-dupe on touch-start without replaying the note `PianoKey`'s own touch-start handler already plays (prevents a double note on the first key). Mouse glissando path left fully intact per "mouse unchanged" acceptance. Note: touched a 3rd file beyond the plan's listed two — `PianoKey.tsx` gained an additive `isGlissandoPressed` prop, required to satisfy the "visual pressed states follow the finger" acceptance clause (keys the finger slides onto never get their own touch-start, so they can't light themselves). Change is additive and does not affect mouse/MIDI behavior. `npm run validate` green (typecheck, lint 0 warnings, 88 tests).

- 2026-07-16: P4-1 done. Deleted verified-dead surface area in one pass, confirming zero live references (grep + `tsc -b` + `noUnusedLocals`) before each removal. **musicTheory.ts:** removed the `FREQUENCIES`/`BASE_FREQUENCIES` tables, the enharmonic subsystem (`getEnharmonicSpelling` + its private `ENHARMONIC_PAIRS`/`SHARP_KEYS`/`FLAT_KEYS` supports — all orphaned once the function goes), `getRomanNumeralForChord`, `getNotesForChord`, and the entire 88-key block (`midiToNoteName`, `generate88KeyPiano`, `createPianoKeyMap`, `createMidiKeyMap`, `getKeyRange`, `noteToMidi`, `PIANO_RANGES`); kept `midiToFrequency` (canonical, P4-2 target) and `isBlackKey`; dropped the now-unused `FrequencyMap`/`PianoKeyData`/`NoteWithOctave` type imports. **types/music.ts:** replaced the dead duplicate `PianoKeyData` (angle/x/y shape) with the live pianoUtils shape (note/baseNote/octave/midiNumber/frequency/isBlack/whiteKeyIndex). **pianoUtils.ts:** removed its local `PianoKeyData` and now imports it from `types/music`; **PianoKey.tsx:** import updated to pull `PianoKeyData` from `types/music`. **MusicContext.tsx:** removed the dead `pianoRange`/`setPianoRange` state+action+reducer surface, `toggleScaleView`, and `deselectChords` (+ the `SET_PIANO_RANGE`/`DESELECT_CHORDS` action variants and the now-unused `setScaleViewEnabled` destructure from useSettings). **useMidiInput.ts:** dropped the unused `stopAllNotes` (and its `useCallback` import) and `isSupported` returns (state binding narrowed to `const [, setIsSupported]`, keeping the support-detection side effect). **ChordStrip.tsx:** removed the dead `layout="sidebar"` branch and the `ChordStripProps`/`layout` prop (only call site is `<ChordStrip />`). **LearnMode.css:** deleted the dead "Desktop Sidebar Layout" section to EOF (`.learn-mode-sidebar`, `.chord-sidebar`, `.sidebar-close-btn`, `.sidebar-resize-*`, `.piano-main`, `.sidebar-fab`, `.sidebar-backdrop` — all 0 non-CSS refs). **package.json:** removed the broken `check:client`/`check:all` scripts (reference nonexistent `scripts/check-client.mjs`) and the unconfigured `lint-staged` devDep. **Test cleanup:** deleted the `midiToNoteName` and `noteToMidi` describe blocks (12 cases) + their imports from `musicTheory.test.ts` — test count 161 → 149 (musicTheory.test.ts 90 → 78). `npm run validate` green (tsc -b, lint 0 warnings, 149 tests) and the Playwright smoke check passed on the user's already-running dev server (5173, reused and left running): app renders, 0 console errors across the session, clicked chord card "I C" (played), clicked piano key C4 (responded), opened the settings drawer — all clean. **In-scope discrepancies noted for follow-up (NOT touched, out of P4-1's narrowed scope):** (a) `MAJOR_SCALE_SPELLINGS`/`MINOR_SCALE_SPELLINGS` (musicTheory.ts) are now dead exports — their only consumer was `getEnharmonicSpelling` — but P4-3 explicitly plans to relocate them to `config/scales.ts`, so left in place per scope (exported, so `noUnusedLocals` does not flag them); P4-3 should reassess whether to move dead data or drop it. (b) `isBlackKey` (musicTheory.ts) is now an orphaned export (only `generate88KeyPiano` used it) — kept, not in P4-1's enumerated scope. (c) `SettingsContext.setScaleViewEnabled` is now unconsumed (MusicContext was its only user) — left alone since SettingsContext.tsx is not in P4-1's file list. (d) `ChordStrip.css` classes for the removed sidebar branch (`.chord-strip-sidebar`, `.chord-cards-vertical`) are now orphaned — scope only names LearnMode.css dead CSS + the JSX branch, so ChordStrip.css was not modified; candidate for the P4-4 CSS pass. **Lockfile note:** `lint-staged` was removed from package.json devDependencies but its entry remains in package-lock.json; `npm install` would prune it. `validate` does not check lockfile sync, so this does not affect the green run.

- 2026-07-16: P4-3 done (commit 39559be). Moved the static domain-data tables out of `musicTheory.ts` into `config/`: `NOTES`, `SCALES`, `MAJOR_SCALE_SPELLINGS`, `MINOR_SCALE_SPELLINGS` → new `src/config/scales.ts`; `CHORD_TYPES` → new `src/config/diatonicChords.ts` (establishing config/ = data, utils/ = behavior, types/ = shapes). `musicTheory.ts` now imports these four/one tables from config for internal use and **re-exports them** (`export { NOTES, SCALES, MAJOR_SCALE_SPELLINGS, MINOR_SCALE_SPELLINGS, CHORD_TYPES }`) so existing consumers (ConfigBar.tsx, ChordCard.tsx, Piano.tsx, and both test files) keep importing from `../utils/musicTheory` unchanged — chose backward-compatible re-export over rewriting every importer to minimize touched files/regression risk; `musicTheory.ts` contains no top-level data-table *definitions* now, satisfying the acceptance criterion. Dropped the now-unused `ChordData` type import from musicTheory (it only annotated `CHORD_TYPES`, which moved). **Table-drove `getBorrowedChords`:** replaced the eight hand-copied `push` blocks with a `BORROWED_CHORDS: Record<Mode, Array<{ numeral; semitoneOffset; intervals; type }>>` table plus a `.map` computing `rootNote: NOTES[(rootIndex + semitoneOffset) % 12]`. **Preserved P3-9's exact chord output** — minor branch still returns I/IV/vi/vii° with identical roots/intervals/types (verified: A minor → A maj, D maj, F# min, G# dim); major branch roots/intervals/types unchanged. **Deliberate assertion change (plan-sanctioned):** standardized the major-branch numerals from ASCII `bVI`/`bVII`/`bIII` to the `♭` glyph (`♭VI`/`♭VII`/`♭III`) to match the rest of the app (getScaleDegreeLabel etc.); updated the three corresponding assertions in `getBorrowedChords.test.ts` — the only test edits, no other assertion touched, and `numeral: 'iv'` (already lowercase, no accidental) was left as-is. Grepped for `bVI`/`bVII`/`bIII` in runtime `src/` (excluding tests): zero hits, so no component special-cased the old ASCII numerals. `npm run validate` green (tsc -b, lint 0 warnings, 149 tests — all pass, incl. the 11 getBorrowedChords cases and 78 musicTheory cases unchanged besides the glyphs). Browser smoke check passed on the user's already-running dev server (5173, reused and left running): app renders, 0 console errors across the session (the 2 warnings are pre-existing Chrome MidiSysEx permission notices, unrelated), borrowed chords now render `♭VI G#` / `♭VII A#` / `♭III D#` in the UI, clicked chord cards "I C" and "♭VI G#" (played, no errors), clicked piano key E4 (responded), settings drawer present/open. **Note carried forward from P4-1 finding (a):** `MAJOR_SCALE_SPELLINGS`/`MINOR_SCALE_SPELLINGS` remain dead exports (no consumers anywhere) even after relocation — P4-3's narrowed scope is to MOVE the tables, not delete data, so they were relocated as planned; a follow-up (post-P4 or the user's regression pass) may choose to drop them entirely. **Lockfile note (still open from P4-1):** `lint-staged` remains in package-lock.json; unaffected by this task.

- 2026-07-16: P4-4 done. CSS hygiene batch, seven verified fixes: (1) **Keyframes** — moved `fadeIn` (was duplicated in `ConfigBar.css` and `LoadingOverlay.css`) and `pulse` (was only in `LoadingOverlay.css`, but `Piano.tsx`'s MIDI dot already depended on it cross-file) into a new "Shared keyframes" section in `index.css`; deleted both local duplicates. Each keyframe name now defined exactly once (grepped to confirm). (2) **Z-index tokens** — added `--z-header: 100`, `--z-drawer: 300`, `--z-overlay: 9999`, `--z-onboarding: 10000`, `--z-toast: 10010` to `index.css`. Replaced the magic numbers in the P4-4 file scope: `ConfigBar.css` header (100→`--z-header`), drawer-backdrop and settings-drawer (200/300→`--z-drawer` — verified both render correctly with the same token because they're DOM-order siblings in the same stacking context, so the later-painted `.settings-drawer` still wins visually; no separate offset needed), `LoadingOverlay.css` (9999→`var(--z-overlay, 9999)`, matching the file's existing fallback-var convention), and the Piano MIDI badge's inline `zIndex: 10` (now `var(--z-header)` in the extracted CSS class — a local stacking value with no other positioned siblings to conflict with, so 10→100 has no visible effect). **Left untouched (out of P4-4's file scope, not in the Files: list):** `LearnMode.css`'s two local `z-index: 10` values and `OnboardingOverlay.css`'s `9999`/`10000` — confirmed via a targeted stacking-context check that these are self-contained within their own component subtrees and don't collide with the newly tokenized layers; noting them here as candidates for a future pass since `--z-onboarding`/`--z-toast` exist precisely for that file once it's in scope. (3) Fixed `var(--text)` (undefined custom property, silently falling back to inherited `--text-primary`) → `var(--text-primary)` explicitly in `VolumeSlider.css:175`. (4) Added a global `@media (prefers-reduced-motion: reduce)` block in `index.css` collapsing all animation/transition durations to near-zero and disabling iteration repeats — covers the loading overlay's `pulse`, the MIDI dot's `pulse`, and every transition app-wide (onboarding already had its own equivalent block; this one is the missing app-wide floor). (5) **Touch targets** — `ChordCard.css`'s `.chord-card-modifier-btn` base rule now uses `min-height: var(--size-touch-target, 44px)` (new token in `index.css`, mirrored by a comment in `config/ui.ts` pointing at `SIZES.minTouchTarget = 44` — no build-time binding exists between CSS and TS in this codebase, so the two are documented as parallel sources of truth rather than mechanically linked); removed the downstream overrides that were dragging it back down to 36px (`pointer: coarse`) and 28px (`max-width: 768px`), so 44px now floors every breakpoint that matters for touch. Verified via Playwright at a 375px viewport: computed `min-height` went from 28px → 44px (rendered button size was already 47×47 due to the 3-column grid's aspect-ratio math, so no visible size change at that viewport — the floor only bites on narrower grids/larger gaps). Deliberately left the desktop-only `min-width: 1024px` rule's 36px alone (not a touch breakpoint, not cited in the roadmap's problem list). (6) **Contrast** — lightened `--text-muted` from `#6e7681` to `#8b949e` (same value as `--text-tertiary`; roadmap's suggested fix). Computed contrast against `--bg` (`#0d1117`): 4.12:1 → 6.15:1, comfortably clearing the 4.5:1 AA floor for the small text that consumes it (`.onboarding-skip`, `.about-credit`). (7) **MIDI badge extraction** — moved the ~25 lines of inline styles (raw hex, magic z-index) out of `Piano.tsx` into two new classes in `Piano.css` (`.midi-badge`, `.midi-badge-dot`), using `--space-1`/`--space-2`/`--radius-sm`/`--z-header` tokens where they matched exactly (padding, position, border-radius) and keeping the handful of one-off pixel values (`12px` font, `6px` gap, `8px` dot) literal since no existing token matches them and inventing new ones was out of this task's scope. Visually pixel-identical to the original inline styles (confirmed by re-deriving each value). Not screenshot-testable in this environment: `midi.isConnected` requires a live Web MIDI device, unavailable to headless Playwright — same limitation noted in earlier P4 sessions. `npm run validate` green (tsc -b, lint 0 warnings, 149 tests unchanged). Playwright before/after screenshots at desktop (1400×900) and mobile (375×700) viewports confirmed the only visible diff is the `--text-muted` lightening (verified numerically, subtle at screenshot scale) — the main scale-chords layout, borrowed-chord row, and modifier-button grid are pixel-identical before/after. Smoke check: app renders, 0 console errors (2 pre-existing MidiSysEx warnings, unrelated), clicked chord card "I C" (selected state applied, no errors), clicked a piano key (responded), settings drawer opened/closed cleanly. Dev server was already running on the user's machine (port 5173); reused and left running.
